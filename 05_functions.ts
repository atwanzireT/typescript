// function to return a number
// Take in a number parameter
function addTwo(num: number) : number{
    return num + 2;
}

// function 
// Take in a string parameter
function getUpper(val: String){
    return val.toUpperCase;
}

let login = (name : String, age : number, hasPaid : Boolean) =>{};

console.log(addTwo(5))
console.log("Done ...")
export {}